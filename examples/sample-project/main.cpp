# Proyecto de ejemplo del framework Satellite (FASE 22)
# Repositorio CONSUMIDOR: el framework se instala externamente y se usa con `satellite init`.

int main()
{
    // Código de ejemplo: el framework no asume nada de este repositorio.
    int a = 2;
    int b = 3;
    return a + b;
}
